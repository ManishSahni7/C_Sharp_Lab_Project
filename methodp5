using System;

class Program
{
    static int Max(int a, int b)
    {
        if(a > b)
            return a;
        else
            return b;
    }

    static void Main()
    {
        Console.WriteLine(Max(10,20));
    }
}
