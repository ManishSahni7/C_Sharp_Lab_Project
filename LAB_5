Definition
Write a program to find all prime numbers between two values inputted by user.
 Definition:

This program takes two numbers from the user and prints all prime numbers between them.
A prime number is a number that is divisible only by 1 and itself.

C# Program
using System;

class Program
{
    static void Main(string[] args)
    {
        int start, end;

        Console.Write("Enter starting number: ");
        start = Convert.ToInt32(Console.ReadLine());

        Console.Write("Enter ending number: ");
        end = Convert.ToInt32(Console.ReadLine());

        for (int i = start; i <= end; i++)
        {
            int count = 0;

            for (int j = 1; j <= i; j++)
            {
                if (i % j == 0)
                    count++;
            }

            if (count == 2)
                Console.WriteLine(i);
        }
    }
}
Definition
Write a program to calculate sum of number inputted by the user.
 Definition:

This program takes a number from the user and calculates the sum from 1 to that number.

Example:
If user enters 5 → Sum = 1+2+3+4+5 = 15

 C# Program
using System;

class Program
{
    static void Main(string[] args)
    {
        int num, sum = 0;

        Console.Write("Enter a number: ");
        num = Convert.ToInt32(Console.ReadLine());

        for (int i = 1; i <= num; i++)
        {
            sum = sum + i;
        }

        Console.WriteLine("Sum = " + sum);
    }
}
 Definition
 Write a program to find minimum of three numbers using conditional operator.
Definition:

This program takes three numbers and finds the smallest number using the conditional (ternary) operator ? :.

C# Program
using System;

class Program
{
    static void Main(string[] args)
    {
        int a, b, c, min;

        Console.Write("Enter three numbers: ");
        a = Convert.ToInt32(Console.ReadLine());
        b = Convert.ToInt32(Console.ReadLine());
        c = Convert.ToInt32(Console.ReadLine());

        min = (a < b) ? 
              ((a < c) ? a : c) : 
              ((b < c) ? b : c);

        Console.WriteLine("Minimum number = " + min);
    }
}
